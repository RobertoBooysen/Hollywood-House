<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >
    <!-- This permission allows the app to read files from the external storage, such as retrieving or opening files(a_local_nobody,2022) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" /> <!-- This permission allows the app to write or modify files on the external storage, such as creating, modifying, or deleting files(a_local_nobody,2022) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.OPSC6311_Task3"
        tools:targetApi="31" >
        <activity
            android:name=".MovieDisplay"
            android:exported="false" />
        <activity
            android:name=".DeleteCategory"
            android:exported="false" />
        <activity
            android:name=".GoalProgress"
            android:exported="false" />
        <activity
            android:name=".Goals"
            android:exported="false" />
        <activity
            android:name=".AddMovie"
            android:exported="false" />
        <activity
            android:name=".AddCategory"
            android:exported="false" />
        <activity
            android:name=".Categories"
            android:exported="false" />
        <activity
            android:name=".Welcome"
            android:exported="false" />
        <activity
            android:name=".Login"
            android:exported="false" />
        <activity
            android:name=".Register"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity
            android:name=".SplashScreen"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>